extends ../layout

block content 
  h1 タスクの編集

  form.box(action=`/teams/${team.id}/tasks/${task.id}`,method="post")
    _csrf
    _method put

    div
      label(for="title") タイトル
      input#title(name="title",value=task.title)
    div  
      label(for="body") 内容
      textarea#body(name="body")=task.body
    div.actions
      input(type="submit") 